// 获取模型的实际尺寸

// const box = new THREE.Box3().setFromObject(model)

// const size = new THREE.Vector3()

// box.getSize(size) // 获取模型的宽、高、深

// console.log('%c Line:35 🍏 size', 'color:#b03734', size)

// 设置模型的缩放、位置和旋转
// model.scale.set(0.3, 0.3, 0.3)

// console.log('%c Line:37 🍇 scene', 'color:#33a5ff', scene)

// const boxHelper = new THREE.BoxHelper(model, 0xFF0000)

// scene.add(boxHelper)

// const axesHelper = new THREE.AxesHelper(5)

// scene.add(axesHelper)

// 应用缩放比例
// model.scale.set(3, 3, 3)

// model.position.set(0, 0, 0) // 将模型移到场景的中心

// scene.add(model)
